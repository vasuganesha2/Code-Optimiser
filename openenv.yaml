name: compiler-optimization-env
version: "1.0.0"
entry_point: serve:app

tasks:
  - name: easy
    grader: env.graders.grade_easy
  - name: medium
    grader: env.graders.grade_medium
  - name: hard
    grader: env.graders.grade_hard

endpoints:
  reset: /reset
  step:  /step
  state: /state
